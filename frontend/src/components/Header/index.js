import React from 'react';

import Navigation from '../Navigation';
import './styles.css';

const header = props => {
  return (
    <div className="Header">
      Header
      <Navigation />
    </div>
  )
};

export default header;